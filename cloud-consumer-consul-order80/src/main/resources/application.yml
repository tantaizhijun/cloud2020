
server:
  port: 80

spring:
  application:
    name: cloud-consumer-order
#    consul注册中心的信息
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}